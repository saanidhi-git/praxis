# Keep the npm cache on D: — the C: drive is nearly full on this machine.
# Project-local so no global npm config is modified.
cache=D:\\KPMG\\praxis\\.npm-cache
fund=false
audit=false
save-exact=true
